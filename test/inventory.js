const chai = require('chai');
const assert = require('chai').assert;
const expect = require('chai').expect;


describe('Inventory', () => {

  context('validiate item price', () => {
    it('price must be greater than $0', () => {
      assert(0);
    })

    it('price must be of type "number", with at most two digits', () => {
      assert(0);
    })
  })

  context('modify inventory', () => {
    it('adding item, places it in inventory', () => {
      assert(0);
    })

    it('request to remove upc, removes item from inventory', () => {
      assert(0);
    })
  })

  context('modify exisiting item', () => {
    it('update price', () => {
      assert(0);
    })

    it('update special', () => {
      assert(0);
    })

    it('update item markdown', () => {
      assert(0);
    })

  })

});
